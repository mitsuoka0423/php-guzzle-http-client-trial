{
    "require": {
        "guzzlehttp/guzzle": "^7.10"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "serve-test-server": "php -S localhost:8000 -t server/",
        "run-tests": "php index.php | tee execution.log"
    }
}
